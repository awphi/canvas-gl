{
  "name": "canvas-gl",
  "version": "1.0.0",
  "description": "Canvas-like API for drawing with WebGL",
  "author": "adamwph",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "readme": "../README.md",
  "scripts": {
    "prepare": "npm run build",
    "build": "esbuild src/index.ts --format=esm --sourcemap --bundle --outdir=dist && tsc"
  },
  "devDependencies": {
    "esbuild": "^0.17.13",
    "typescript": "^4.9.3"
  },
  "dependencies": {
    "parse-css-color": "^0.2.1",
    "regl": "^2.1.0"
  }
}
